---
- hosts: 10.1.0.5
  become: yes
  tasks:
  - name: i want to excute direct linux command
    command: hostname -f 
    register: myfirsttaskresult  
 
  - name: i want to debug first task 
    debug: 
      msg: "{{myfirsttaskresult}}"